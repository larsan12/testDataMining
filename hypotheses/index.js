const Combinatorics = require('js-combinatorics')
const Ext = require('./ext')
const Compare = require('./compare')

/**
 * interface:
 * class {
 * 
 *      constructor(data)
 * 
 *      getCombs(m)
 * 
 *      checkAll(comb, indeces)
 * 
 * }
 */



module.exports = {
    Ext,
    Compare
}